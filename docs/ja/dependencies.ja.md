# 依存ライブラリ一覧

本ドキュメントはルール18に基づき、scliが使用するすべてのサードパーティ依存ライブラリを記録します。

---

## 直接依存

### `github.com/spf13/cobra`

| 項目 | 内容 |
|------|------|
| バージョン | v1.10.2 |
| ライセンス | Apache-2.0 |
| 目的 | CLIフレームワーク — コマンド/サブコマンドのディスパッチ、フラグ解析、ヘルプ生成 |
| 内製しない理由 | GoのCLI開発における事実上の標準ライブラリ。サブコマンドルーティング、フラグ継承、ヘルプ生成、シェル補完を再実装することは、メリットのない大きな工数となる。 |

---

## 間接依存

直接依存から引き込まれるライブラリ。scliのソースコードから直接インポートはしない。

### `github.com/fatih/color`

| 項目 | 内容 |
|------|------|
| バージョン | v1.19.0 |
| ライセンス | MIT |
| 目的 | ANSIカラー出力、全プラットフォームでのTTY自動検出 |
| 内製しない理由 | WindowsのANSIエミュレーションとisattyチェックをプラットフォーム横断で処理する。これを内製するとOSごとのコードが約200行増える。 |

### `github.com/zalando/go-keyring`

| 項目 | 内容 |
|------|------|
| バージョン | v0.2.6 |
| ライセンス | MIT |
| 目的 | クロスプラットフォームのOSキーチェーンアクセス（macOS Keychain / Linux libsecret / Windows Credential Manager） |
| 内製しない理由 | プラットフォームごとに異なるネイティブAPI（Security.framework、dbus/libsecret、wincred）が必要。内製するとこのライブラリをそのまま複製することになる。 |

### `github.com/joho/godotenv`

| 項目 | 内容 |
|------|------|
| バージョン | v1.5.1 |
| ライセンス | MIT |
| 目的 | 設定レイヤーのトークンフォールバックに使用する `.env` ファイルの解析 |
| 内製しない理由 | `.env` ファイルの解析にはクォートやコメントなど多くのエッジケースがある。セキュリティ上重要な処理に実績あるライブラリを使うことで微妙なバグを防ぐ。 |

### `github.com/danieljoos/wincred`

| 項目 | 内容 |
|------|------|
| バージョン | v1.2.2 |
| ライセンス | MIT |
| 目的 | `go-keyring` が使用するWindows Credential Managerバックエンド |
| 備考 | 間接依存のみ。直接インポートなし |

### `github.com/godbus/dbus/v5`

| 項目 | 内容 |
|------|------|
| バージョン | v5.1.0 |
| ライセンス | BSD-2-Clause |
| 目的 | Linux上でlibsecretにアクセスするために `go-keyring` が使用するD-Bus IPC |
| 備考 | 間接依存のみ。直接インポートなし |

### `al.essio.dev/pkg/shellescape`

| 項目 | 内容 |
|------|------|
| バージョン | v1.5.1 |
| ライセンス | MIT |
| 目的 | `go-keyring` が使用するシェルセーフな文字列エスケープ |
| 備考 | 間接依存のみ。直接インポートなし |

### `github.com/mattn/go-colorable` / `github.com/mattn/go-isatty`

| 項目 | 内容 |
|------|------|
| バージョン | v0.1.14 / v0.0.20 |
| ライセンス | MIT |
| 目的 | `fatih/color` が使用するWindowsのstdoutカラーサポートとTTY検出 |
| 備考 | 間接依存のみ。直接インポートなし |

### `github.com/spf13/pflag`

| 項目 | 内容 |
|------|------|
| バージョン | v1.0.9 |
| ライセンス | BSD-3-Clause |
| 目的 | cobraが内部で使用するPOSIXスタイルのフラグ解析 |
| 備考 | 間接依存のみ。直接インポートなし |

### `github.com/inconshreveable/mousetrap`

| 項目 | 内容 |
|------|------|
| バージョン | v1.1.0 |
| ライセンス | Apache-2.0 |
| 目的 | Explorerからバイナリを直接起動したユーザーへの警告のために cobraが使用するWindowsダブルクリック検出 |
| 備考 | 間接依存のみ。直接インポートなし |

### `golang.org/x/sys`

| 項目 | 内容 |
|------|------|
| バージョン | v0.42.0 |
| ライセンス | BSD-3-Clause |
| 目的 | `go-isatty` と `go-keyring` が使用する低レベルOSシステムコールヘルパー |
| 備考 | 間接依存のみ。直接インポートなし |

---

## 開発ツール（バイナリには含まれない）

| ツール | 目的 |
|--------|------|
| `golangci-lint` | 静的解析・lintゲート（`make check`） |
| `govulncheck` | 依存グラフの脆弱性スキャン（`make check`） |

---

*最終更新: 2026-03-20*

*原文（英語）: `docs/en/dependencies.md`*
